$(document).ready(function (){
    // this depends on the id's used for different tab-panes in modify.html (Basic Theme)
    $('#meta, #help').removeClass('active');
    $('#f_content_form_data_text, #f_extra_meta_text').autosize();
});
