SoC 2010: Wiki-like-formats-support MoinMoin:DmitryAndreev
==========================================================

Version 2.0:

New features:
  * Add DOM->Moinwiki converter.
  * Add support conversion of a ReStructuredText conversion document into the internal DOM tree.
    Directives which adds "classes" attribute are ignored.
    * ReStructuredText->DOM converter.
    * DOM->ReStructuredText converter.
  * Add support conversion of a Mediawiki document into the internal DOM tree
    * Mediawiki->DOM converter.
      Converter is based on Moinwiki->DOM parsing model.
      This converter doesn't give full support of mediawiki syntax
